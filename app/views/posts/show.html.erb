<h1><%= @post.title %></h1>
By: <%= image_tag @post.user.avatar.thumb %> <%= @post.user.first_name %>

<div>
  <p><%= @post.body %></p>
</div>

<% @post.post_images.each do |img| %>
  <div>
    <%= img.title %>
    <%= image_tag img.url %>
  </div>
<% end %>
